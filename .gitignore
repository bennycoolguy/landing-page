# NEVER commit raw lead data (owner names + pipeline). Encrypted versions are OK.
data/leads/*.json
!data/leads/*.enc.json

# Backup/original asset versions
images/*-original.png

# macOS
.DS_Store
Icon?

# Server logs
*.log
